# GitHub Pages does not serve Git LFS objects: an LFS-tracked video is served
# as its pointer text file and will not play. The system git config enables the
# LFS filter for this machine, so disable it here for media that must be
# fetchable directly over HTTP.
*.mp4 -filter binary
*.jpg -filter binary
*.png -filter binary
